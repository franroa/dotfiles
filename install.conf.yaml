- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.bashrc: bashrc
    ~/.config/alacritty/.alacritty.yml: alacritty

- shell:
    - [git submodule update --init --recursive, Installing submodules]
